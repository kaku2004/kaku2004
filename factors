/* Write a program to display the factors of an input number */

#include<stdio.h>
int main()
{
int i,n;
printf("Input a number");
scanf("%d", &n);
for(i=2; i<=n/2; i++)
if (n%i==0)
printf("The factors are=%d\n",i);
return 1;
}
