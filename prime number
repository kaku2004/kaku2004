/* Write a program to check whether an input number is prime or not */

#include<stdio.h>
int main()
{
int i,n;
printf("Input a number");
scanf("%d", &n);
for(i=2;i<=n/2;i++)
if(n%i==0)
 break;
if(i<=n/2)
 printf("%d is a composite number", n);
else
 printf("%d is a prime number", n);
 return 1;
}
