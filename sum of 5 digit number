/* Write a program to find the sum of a five digit number */

#include<stdio.h>
int main()
{
int n, sum;
printf("Input a five digit number");
scanf("%5d", &n);
sum=n%10;
n=n/10;
sum=sum+n%10;
n=n/10;
sum+=n%10;
n=n/10;
sum+=n%10;
n=n/10;
sum+=n%10;
printf("Sum of digits=%d",sum);
return 1;
}
